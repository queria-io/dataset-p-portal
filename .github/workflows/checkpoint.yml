name: Checkpoint

on:
  schedule:
    - cron: "0 18 1 * *" # 毎月1日 03:00 JST
  workflow_dispatch:

jobs:
  checkpoint:
    uses: queria-io/dataset-shared/.github/workflows/checkpoint.yml@main
    secrets: inherit
