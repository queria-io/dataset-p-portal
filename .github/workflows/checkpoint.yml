name: Checkpoint

on:
  schedule:
    - cron: "0 18 1 * *" # 毎月1日 03:00 JST
  workflow_dispatch:

jobs:
  checkpoint:
    uses: flo8s/dataset-shared/.github/workflows/checkpoint.yml@main
    with:
      s3_bucket: ${{ vars.S3_BUCKET }}
    secrets: inherit
