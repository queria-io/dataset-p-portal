[project]
name = "dataset-p-portal"
version = "0.1.0"
description = "Procurement Portal (GEPS) award records dataset for fdl"
requires-python = ">=3.13"
dependencies = [
    "frozen-ducklake>=0.10.3,<0.11",
    "dbt-core>=1.10.0",
    "dbt-duckdb>=1.10.0",
]
